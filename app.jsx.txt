import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { motion } from "framer-motion";
import { Calendar, Shield, Wrench, Cpu } from "lucide-react";

export default function ITSupportSite() {
  return (
    <div className="min-h-screen bg-gray-50 text-gray-900 flex flex-col">
      {/* Hero Section */}
      <section className="bg-blue-600 text-white text-center py-20 px-6">
        <motion.h1 initial={{ opacity: 0, y: -20 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.6 }} className="text-4xl font-bold mb-4">
          Your On-Call IT Guy
        </motion.h1>
        <motion.p initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 0.3, duration: 0.6 }} className="text-lg mb-6 max-w-xl mx-auto">
          Remote troubleshooting, security advice, and PC tune-ups — fast and friendly.
        </motion.p>
        <Button className="bg-white text-blue-600 hover:bg-gray-200 text-lg px-6 py-3 rounded-2xl shadow">
          Book a Free Consultation
        </Button>
      </section>

      {/* Services Section */}
      <section className="py-16 px-6 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 max-w-6xl mx-auto">
        <ServiceCard icon={<Wrench className="w-8 h-8 text-blue-600" />} title="Remote IT Support" description="Fix software issues, network errors, and tech headaches remotely." />
        <ServiceCard icon={<Shield className="w-8 h-8 text-blue-600" />} title="Cybersecurity Tips" description="Password audits, best practices, and simple security coaching." />
        <ServiceCard icon={<Cpu className="w-8 h-8 text-blue-600" />} title="PC Optimization" description="Speed up slow computers with cleanup & performance tuning." />
        <ServiceCard icon={<Calendar className="w-8 h-8 text-blue-600" />} title="General Tech Help" description="Installations, updates, and troubleshooting — on demand." />
      </section>

      {/* Why Choose Me */}
      <section className="bg-white py-12 px-6 text-center shadow-inner">
        <h2 className="text-2xl font-semibold mb-4">Why Choose Me?</h2>
        <p className="max-w-2xl mx-auto text-gray-700 mb-6">
          I’m an IT professional with years of experience helping users get back to work quickly. My goal is to make tech simple and stress-free for you.
        </p>
        <ul className="space-y-2 text-gray-800">
          <li>✅ Fast remote help</li>
          <li>✅ Affordable flat-rate services</li>
          <li>✅ Friendly and professional</li>
        </ul>
      </section>

      {/* Contact / Booking Section */}
      <section className="py-12 px-6 text-center">
        <h2 className="text-2xl font-semibold mb-4">Book Your Free Consultation</h2>
        <p className="mb-6 text-gray-700">Pick a time that works for you and let's get your tech running smoothly.</p>
        <a href="https://calendly.com/yourusername/consultation" target="_blank" rel="noopener noreferrer">
          <Button className="bg-blue-600 text-white hover:bg-blue-700 text-lg px-6 py-3 rounded-2xl shadow">
            Schedule Now
          </Button>
        </a>
      </section>

      {/* Footer */}
      <footer className="bg-gray-800 text-white py-6 text-center text-sm">
        <p>© {new Date().getFullYear()} Your Name – IT Support & Consulting</p>
        <p className="text-gray-400">Contact: youremail@example.com</p>
      </footer>
    </div>
  );
}

function ServiceCard({ icon, title, description }) {
  return (
    <Card className="shadow-lg rounded-2xl">
      <CardContent className="p-6 text-center">
        <div className="flex justify-center mb-4">{icon}</div>
        <h3 className="text-xl font-semibold mb-2">{title}</h3>
        <p className="text-gray-600">{description}</p>
      </CardContent>
    </Card>
  );
}
